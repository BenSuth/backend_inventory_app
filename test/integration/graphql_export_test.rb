require "test_helper"

class GraphqlExportTest < ActionDispatch::IntegrationTest
  # Because of time constraints, this is still left TODO,
  # It would follow exactly the same as the graphql_item_test
  # Tests would ensure queries are returned as well as ensure
  # that constraint on external_name (not empty) is not violated
end
